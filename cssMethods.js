const cssId = (elemId) => document.getElementById(elemId).style;

const cssClass = (className) => document.getElementsByClassName(className).style;

const cssTag = (tag) => document.getElementsByTagName(tag).style;

// INSERT METHOD